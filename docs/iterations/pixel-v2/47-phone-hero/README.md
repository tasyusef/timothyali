# PX-47 — The hero on a phone

2026-09-22, decision 0127. `before/` is the PX-43 hero as deployed, `after/` the reworked one, both first-screen captures on the preview build at 375×667, 390×844 and 430×932 (mobile emulation) plus 1440×900. In `after/` the hero ends at the fold on every phone, the status is two clean lines at 480px and below, and the résumé link sits beside the “Selected work” chip. Svelte check 0/0 and the production build pass. Superseded the same evening by PX-48, which keeps the hero height, the actions row and the clock breakpoint; pushed with it.
