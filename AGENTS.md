# Personal rebrand and positioning

## Current direction — pixel / type alternate (0044–0053)

Timothy originally requested keeping the yellow/black palette (since revised to black/white with yellow as accent, 0049), pixel Jacquard blackletter, name, tone and identity while replacing the city direction. The active site now uses programmatic, Swiss-influenced typography and layout, a pixel motif, restrained ASCII detail and stepped kinetic type. No 3D shapes or Three.js runtime remain. Home, Work and Contact share the alternate system. Timothy confirmed the pixel-grid direction in 0047 (“much better than what we had before”) and asked for yellow on black, no Swiss hairlines, a quieter hero with only the name over texture, texture backgrounds on the first two bands, cards for the work, and no section eyebrows; PX-04 implemented that; PX-05 (0048) then put texture on every band, added a per-line text knockout so type stays readable on textures, merged the middle section into one band, restored the hero Timothy liked, and passed a grid audit (canvases snapped to the page grid, integer text boxes). PX-06 (0049) then replaced the yellow ground with black and white plus a theme toggle, kept yellow as the accent in three places, and made photographs full colour on the 2px grid. PX-07 (0051, built 2026-09-09 from `docs/handoff-px07.md`) then cut “I get invested.” after a mocked comparison (0052) then folded the statement and the three words into one annotated sentence after a second round of mocks (0053: “Designer for teams that don’t have one yet. I care how it looks. moves. works.”), promoted the cofounder proof into the statement band, dropped the ASCII diagram, gave the cards a “Selected work” heading, and made the scroll hint and card labels fully visible. PX-08 (built 2026-09-09 on Timothy’s “next we need to work on the work page and the case study pages”) replaced the Work page’s duplicate card grid with one wide row per project and added four case studies at `/work/<slug>/` on the pixel system (content ported from the old site). The same evening Timothy directed that images must not be pixelated (0054: `Picture`/`Clip` render at native resolution, textures alone stay pixelated) and that Pocketwatch leave the index and be replaced (0055: Do Androids Dream, the assistant’s pick, is project 04). The layout is an assistant proposal awaiting his review; Q28/Q31 stay open. PX-09 to PX-12 then followed Timothy’s reactions (selected-work introduction 0057, inverted statement panel 0058, continuous rain 0059, nav wordmark and behaviour 0060–0062, yellow discipline labels 0063, Work header and row treatment 0064–0066), PX-13 (2026-09-09) applied the three fixes from his review: an ink plate under the yellow discipline labels so they read on the inverted panel (0067), the Work header stacked below 700px (0068), and the Work-row “View case study” following the body plate instead of being pinned to the bottom of the row (0069), plus the phone header height (0070). PX-14 (2026-09-09, on “then do the design system stuff”) then extracted the design system: tokens in `src/lib/styles/primitives.css` and `tokens.css`, type roles in `type.css`, chrome/layout/blocks split out, `src/app.css` reduced to the cell table and an import list, `src/lib/tokens.ts` for JavaScript, and nine reusable components (`Arrow Cursor Plate Cta IndexRow MetaLine PageFoot QuietLink Band`). The site renders pixel-identically apart from four recorded unifications; the structure, names and unifications are the assistant’s proposals (0071, Q34). PX-15 (2026-09-09) then reworked the mobile chrome on Timothy’s direction: the status strip keeps 16px and sheds items by priority (coordinates ≤1100; Denver, SYS.OK and the path’s middle segments ≤900; the clock ≤420) per 0072, and the footer becomes a settings list of the three toggles below 700px per 0073 (the list layout is the assistant’s proposal). PX-16 (2026-09-09) then stripped the Work rows on Timothy’s pick (“the plates behind the text feels weird” → “kill the surface”): no dither card, no text plates, type on the page ground beside the cover, 64px row gaps, the dither only as a 16px hover mat around the cover; `Plate.svelte` is gone (0074). PX-17 (2026-09-09): Timothy accepted the design system (“everything sounds good to me”, 0075); `--accent-text` (`--yellow-deep` on the light ground), breakpoints 420/700/900/1100, index counters read by screen readers; the third discipline chip reads “Product” on phones (0076). PX-18 (2026-09-09, decision 0077, on “i also want to bring in all the other projects” and “all of them should have case studies”) brought the rest of the old site across: the Work page now carries the whole index in two tiers — the selected four as wide rows, then FirstStrike Research, Sonde, PARC Website and Jade Aesthetics as linked index entries 05–08 — every visible project has an on-site case study, counts run to 008, and Gridform Studio and Studio Gridform stay in the data hidden with no pages and no assets. `src/lib/work.ts` is now one array with a `tier` and a `hidden` flag and derives every number. **The current version is PX-18; decisions run through 0077.** PX-19 (2026-09-10, 0078) put Timothy’s PARC Pixel on the site: Bold for every uppercase display role (55/56, 41.25, 27.5 on phones), Regular for every label (12.5/16); the family was extended with seven glyphs and a new ampersand and the Bold regridded, all by `tools/fonts/extend-parc-pixel.py`; Jersey 25 and Silkscreen are gone; the study titles are Bold too (0079). PX-20 (0080): Pocketwatch is back as index entry 09 with a re-ported study — 0055 had misread “remove from my selected works” as removal from the site; nine studies, counts to 009. **Next session: read `docs/design-system.md` first, then `docs/handoff-px08-review.md`, then the PX-14 entry at the end of `docs/iteration-log.md`.** The system from PX-03 (decision 0046) stands: pages built on one measured pixel grid — bitmap faces sized as cells-per-em × cell (table at the top of `src/app.css`), seeded canvas dither/ASCII textures, dithered work photographs, typed/scrambled/stepping type, a terminal readout strip and a Grid toggle. Awaiting visual review; the supporting faces and chrome copy are proposals (Q31).

PX-22 (2026-09-10, 0081): landing Selected work and closing footer refined on Timothy’s request. Cards have wider gaps, dither image mats and solid captions; the mobile work introduction is more compact; All work and Get in touch share the right desktop column; a Home-only footer variant uses accent states and 48px phone rows. Copy, selected projects and accepted tokens/type roles are preserved. Implemented assistant proposal awaiting visual review. Evidence: `docs/iterations/pixel-v2/22-home-work-footer/`. Existing 320px overflow in the unchanged introductory `.who .display` is recorded in PX-22.

PX-23 (2026-09-10, 0082): new Jacquard uppercase T favicon in ink on yellow (Timothy’s PX-23 correction; SVG/ICO/touch PNG) and twelve per-page OG images in `static/og/`, with server-rendered canonical/OG/Twitter metadata. Regenerate with `pnpm social:generate`, then build and `pnpm social:verify`; see `tools/social/README.md`. Public origin retained from old site: `https://www.timothyali.com`. PX-25 (0084) re-rendered the OG set from a dev-only `/og/[id]` route. Live since 2026-09-10: this folder is the git checkout of `tasyusef/timothyali`; pushing `master` deploys to https://www.timothyali.com via Vercel (0085). Timothy set the further landing typography experiment aside.

PX-24 (2026-09-10, 0083): OG images revised on Timothy’s request for the terminal aesthetic and larger type: actual ASCII glyph rain, Press Start readouts, yellow path strip, 344px Home/Contact blackletter and 82.5px case titles. Current evidence in `docs/iterations/pixel-v2/24-social-terminal/`. Favicon unchanged. Assistant compositions awaiting review.

Keep decisions in `docs/decision-log.md` and meaningful iterations in `docs/iteration-log.md`, with before/after evidence under `docs/iterations/`. Timothy explicitly asked for both. Distinguish his direction from implementation choices and do not label a working design approved without confirmation.

The old city is preserved at `/Users/twocakes/Desktop/PROJECTS/timothyali2-city-v1-2026-09-08`; do not modify that snapshot. Entries below about the city and reduced-motion edition are historical and superseded for the active working site. Read `docs/alternate-v2.md` for the current editing map and `docs/design-system.md` for the tokens, type roles and components.


Saved city version: `/Users/twocakes/Desktop/PROJECTS/timothyali2-city-v1-2026-09-08` is an independent snapshot through correction 0042, including Three.js source, reduced-motion edition, assets, dependencies, build and notes. Keep it intact. Timothy requested this save so an alternate version can be developed; `timothyali2` remains the working copy. No alternate design has been specified yet. The staged Tyrell exploration is preserved separately inside the snapshot.


Correction 0042 makes the reduced-motion and no-JavaScript story type-focused, following Timothy’s latest request after the illustrated still exploration felt weird. Six chapters use a disciplined grid, large lowercase Jacquard emphasis, Anton supporting text, and alternating yellow/black backgrounds. Reduced mode loads no Three.js, uses ordinary page scrolling, and has chapter navigation. Switching modes or responding to the OS preference retains the current chapter; explicit choices persist. This is implemented for review, not visual acceptance. The animated city geometry and camera are unchanged. The Tyrell pyramid remains a separate staged proposal in the import workspace; it has not been applied to the live city.


Correction 0041 refines the opening foreground and separates the introduction from the fixed wordmark. A low angular quay hall and a basin connected to the eastern canal give the lower view waterfront structure; the cross viaduct reaches the far quay. Ground and water shift toward warmer yellows. The large ground surface is subdivided so it no longer visually covers nearby water. The opening type is modestly smaller and its initial world-space position adapts to the viewport, leaving a clear navigation gap and preventing left-edge clipping on taller desktop windows. It stays anchored in the city during scroll. This implements Timothy’s requested correction; it is not visual approval.

Correction 0040 revises the bridge-to-overlook movement after Timothy described its path as awkward. From scene 05, the camera follows a continuous, distance-sampled crane path through the opening and forward over the terrace. It stays farther from the right tower and arrives 56 units deeper into the city. The viewing direction levels gradually toward the skyline, and the desktop lens and portrait lift follow the same travel phase. The invitation moves by the same depth offset to retain its reading scale; final links and the completion label appear at progress .97. This is an implementation response for review, not Timothy’s visual approval.

Refinement 0039 is a whole-city model polish pass requested by Timothy. District towers now use four extruded profiles mixing stepped crowns, clipped roofs, angled shoulders, and split tops. Sparse recessed and lit windows extend up the usable faces, with return/rear detail and silhouette checks. SVG landmarks keep their approved outlines and gain restrained façade accents. Canal banks have coping and finer water marks; road piers have tapered bodies and integrated bearing heads; the junction billboard gains a defined frame and foundations; canyon walls and the terrace arcade have occupied window bays. The terrace adds deck joints, a finished cap, panel joints, and attached underside ribs. This is a reviewed working revision, not Timothy’s visual acceptance. The narrow angular road, lowercase Jacquard emphasis, open underpass, and atmospheric depth remain in place.

Read `docs/codex-handoff.md` first, then the project README and decision log. This is the continuation of the matching Claude project, imported on 2026-09-08.

Use the existing project files in this folder as the source of truth. Respect the latest explicit user direction when older log entries conflict: decisions 0018, 0020, and 0021 govern the name, type, and landing concept.

Preserve decision history. Record proposals as proposals and accepted decisions only on Timothy's confirmation; never invent his reasoning. Be a candid peer. Keep direction-setting focused on the concept and reserve execution details for implementation.

Current stage: The local environment now reflects corrections through 0041. Timothy rejected the visible city edge, floating rails, inverted city/road lettering, boxy bridge surroundings, and unconvincing terrace approach. The working revision adds faceted terrain, replaces scene 04 with an oblique vertical architectural statement, anchors the bridge to angular abutments, grounds the final terrace, corrects antenna-to-roof connections, and delays the final headline until the climb has framed it. The tall canyon sign uses lowercase Jacquard “look.” / “move.” / “work.” as explicitly requested. This is implemented for review; it is not Timothy’s visual approval. Work is direct: Timothy explicitly requested no orchestration skill.

Latest scale correction: Correction 0038 responds to Timothy’s concern that the road is too wide for the intended monumental city scale. The main road is reduced from 42 to 18 world units (about 57% narrower); its depth changes from 8 to 3.5, with finer markings, lower/slimmer barriers, and proportionate piers and footings. The cross viaduct is narrower too. A shared `ROAD_HALF_WIDTH` controls geometry and the distant district’s clearance. One additional camera key follows the narrower bend precisely. The tower and type dimensions remain the same in this scale trial.

Latest type/detail correction (0037): Scene 04 now emphasizes “people use.” in lowercase Jacquard. Scattered window clusters occupy the civic building above and below the lettering bay and along its side return, with matching windows on the neighboring tower. The windows follow the actual building planes; the central typography area remains clear. This follows Timothy’s explicit request for blackletter and scattered windows in the civic lockup.

Latest scene-specific correction: Correction 0036 refines the bridge-to-tower connections and opens the underpass. The bridge ends sit in recessed collars on short bearing seats; the long hanging braces are removed. Tower bases are set outside the road corridor. The obstructing nearby lot is removed, the setback landmark is relocated, and the terrace is carried by side wings around an open arcade. The road continues straight beneath it before turning farther out in the city. Distant district towers now respect a reserved road corridor, and the narrow landmark previously intersecting the bend is moved aside. The city behind the bridge is visible through the opening. This is a working implementation responding to Timothy’s correction, not visual approval.

Latest correction: 0034 supersedes the inverted-city/road-text interpretation of storyboard 03. Keep its dramatic perspective, monolithic architecture, atmospheric depth, angular roads, and mixed SVG profile history. Read `docs/implementation-v1.md` and `docs/vector-world.md` for the current editing map.

The geometry revision passed `pnpm check` (zero errors/warnings), the production build, 125 browser checks covering desktop, 390px/320px and fallback modes, and seven navigation/remount/reverse-scroll checks. Sixteen desktop and sixteen mobile travel positions were captured without browser errors before the final antenna and headline-entry corrections; a targeted desktop .96 capture verified the corrected headline entry. An instrumented browser audit sampled 1,001 actual rendered camera positions per desktop/mobile viewport against candidate solid geometry, using local boxes and ray-intersection parity for extrusions; no camera-center intersections were found. This is a sampled clearance check, not a continuous-volume proof. The final lowercase text edit was confirmed in the live DOM with Jacquard 24. Technical checks support behavior, not visual acceptance.

Latest narrative decision: 0023 accepts the landing introduction and separate Work/case studies, with Home / Work / Contact as the main structure. Read `docs/landing-story.md`. Decision 0024 accepts the main narrative and action labels in `docs/landing-copy-v1.md`; 0028 authorizes the shorter storyboard text as the working v1. The optional founder credit remains open.

Latest visual direction: 0026 calls for grids that can follow scene perspective, heavier display type, flexible casing, and selective Jacquard emphasis. Storyboard 03 and copy v2 inform the working implementation. Do not enforce storyboard 01’s screen-level text or uniformly lowercase styling.

Latest storyboard requirements: 0027 calls for a large futuristic, somewhat dystopian city, distinct 02/03 billboard layouts and 05/06 compositions, and explicit spatial continuity. Decision 0028 authorizes implementing storyboard 03 and its plan/elevation route as v1; the camera route and compositions remain open to visual iteration.

Implementation authorized in 0028: build v1 from storyboard 03 with simplified editable SVG/vector silhouettes closer to the DAD source. The latest shorter storyboard copy can be used as the working version. Generated PNG boards must not become runtime environment assets. Keep old timothyali site untouched.

Latest polish verification (0039): Code checking finished with zero errors or warnings, and the production build passed (with the existing chunk-size advisory). During this pass, 125 browser checks passed across desktop, 390px, 320px, reduced-motion and unavailable-JS/WebGL modes. After the final terrace detail adjustment, all six desktop lockups and sixteen travel positions per desktop/mobile viewport were recaptured without browser errors; seven navigation/remount/reverse-scroll checks passed. The final geometry audit sampled 1,001 camera positions per desktop/mobile viewport without camera-center intersections. A road-width audit sampled 3,378 center/lane-offset positions without solid intersections. These are sampled implementation checks, not continuous-volume proofs or visual acceptance. Decorative instanced windows are excluded from the structural-solid audit.

PX-30 (2026-09-10, 0093): Toolbox landing refined on Timothy’s request for a cleaner, more distinctive second landing page using app components. Large blackletter/shaded-band hero, four-tool selector with one live example, inverted workflow band, release status. Five screenshots replaced by lightweight demos using three copied app controls; export engine stays in the desktop app. Tool catalog and detail pages preserved. `src/lib/components/toolbox/README.md` records provenance. Local implementation and new copy await review; evidence in `docs/iterations/pixel-v2/30-toolbox-landing/`.

PX-31 (2026-09-11, 0094): Toolbox landing polish on Timothy’s review request. Example card flush with the picker, Acme lockup centred and its A redrawn, specimen in all four site faces at cell-table sizes with the block caret, Convert poster authored at two whole-cell sizes, shared `Cursor` in the card bar and terminal. `tools/review/toolbox-grid.mjs` audits block tops and text pixels per tool state. Proposal, uncommitted; evidence in `docs/iterations/pixel-v2/30-toolbox-landing/`.

PX-32 (2026-09-11, 0095): Toolbox examples rebuilt as miniatures of the app's tool screens on Timothy's direction (Codex's 0093 cards did not look or work like the app). `src/lib/components/toolbox/`: `demo.css` (the app's blocks, scoped), `plan.ts` (the app's export plan, tree, palette exporters), one `*Demo.svelte` per tool. Lockup drives a live file count and the real folder tree; Convert encodes in the browser and measures. Checks: `tools/review/toolbox-{demo,grid,landing}.mjs` against `pnpm preview --host 127.0.0.1 --port 4173`. Proposal, uncommitted.
